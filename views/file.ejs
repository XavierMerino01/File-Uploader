<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= file.name %></title>
</head>
<body>
    <% if (!locals.user) {%>
        <h1>Authentication error, please log in.</h1>
        <a href="/">LOG IN</a>
    <% } else { %>
    <h1><%= file.name %></h1>
    <% const fileSizeInKB = (file.size / 1024).toFixed(2); %>
    <h3>Size: <%= fileSizeInKB %> KB</h2>
    <% const date = new Date(file.createdAt); %>
    <p>Date: <%= date.toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'long',
            day: 'numeric',
            hour: '2-digit',
            minute: '2-digit'
        }) %> </p>
    <a href="<%= file.url %>" download="<%= file.name %>">Download</a>
    <a href="/">Home</a>
    <% }%>
</body>
</html>